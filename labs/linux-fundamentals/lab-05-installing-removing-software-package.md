# Lab 01 — installation et suppression de package

## Objectif

utiliser le haut niveau de gestion de paquet correspondant a mon os(apt) pour installer le package dump sur mon systeme et ensuite le supprimer.

## Commandes exécutées

## sudo apt update

## sudo apt install dump

## sudo apt remove dump